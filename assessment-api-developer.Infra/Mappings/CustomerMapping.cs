//using assessment_api_developer.Domain.Models;
//using Microsoft.EntityFrameworkCore;
//using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace assessment_api_developer.Infra.Mappings
{
    //public class CustomerMapping : IEntityTypeConfiguration<Customer>
    //{
    //    public void Configure(EntityTypeBuilder<Customer> builder)
    //    {
    //        builder.ToTable("Customers");
    //        builder.HasKey(x => x.ID);
    //        builder.Property(x => x.Name).IsRequired().HasMaxLength(100).HasColumnType("varchar(100)");
    //        builder.Property(x => x.Email).IsRequired().HasMaxLength(100).HasColumnType("varchar(100)");
    //        builder.Property(x => x.Phone).IsRequired().HasMaxLength(20).HasColumnType("varchar(20)");
    //        builder.Property(x => x.BirthDate).IsRequired().HasColumnType("datetime");
    //        builder.Property(x => x.CreatedAt).IsRequired().HasColumnType("datetime");
    //        builder.Property(x => x.UpdatedAt).IsRequired().HasColumnType("datetime");
    //    }
    //}
}
